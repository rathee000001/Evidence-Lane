# Public Model Package Guide

Exported packages contain locked env/uop, mutable project sectors by explicit command, project router SQLite, sector SQLite files, MMD topology, manifests, receipts, and a flash prompt. Public models must treat the chat window as display-only and package truth as durable state.
